@model IEnumerable<MyWebApp.Models.TB_Video>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DirVideo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Content.ContentType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DirVideo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Content.ContentType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdContent }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdContent }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdContent })
        </td>
    </tr>
}

</table>
